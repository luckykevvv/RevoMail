# Backend quick reference

Run backend commands from the repository root. `npm ci` creates the ignored `.venv` and installs `backend/requirements.txt`; `npm run start` verifies that environment before launching `backend.run` without a development reloader.

See `backend/README.md` and the root `README.md` for complete setup and environment configuration.
